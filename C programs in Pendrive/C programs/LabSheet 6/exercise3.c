#include < stdio.h>
float circleArea( float radius );
float circlePeri( float radius );
float recArea( float length, flaot width );
int main( void )
{
	float h, r;
	float cylHeight, rectArea, circleArea, circlePeremeter;
	float cylinderArea;
	
	printf( "Enter the radius : " );
	scanf( "%f", &r ); 
	
	printf( "Enter the Cylinder height : " );
	scanf( "%f", &h ); 
	
	cylin
	
	
	
	return 0;
}
float circleArea( float radius )
{
	return 2 * ( 7 / 22.0 ) * radius * radius;
}
float circlePeri( float radius )
{

	return 2 * ( 7 / 22.0 ) * radius;
}
float recArea( float length, float width  )
{
	return lenght * width;
}



